/**
 * The BAZ mark: a perched falcon (בז), single colour via `currentColor`.
 * Source file: public/brand/falcon.svg (also the basis of public/favicon.svg
 * and the PNG icons). Bundled as text by the `rules` entry in wrangler.jsonc
 * and inlined into the footer.
 */
import falcon from "../public/brand/falcon.svg";

export const brandMark: string = falcon;
